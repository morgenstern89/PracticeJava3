package practice3;

public class TransformMethodString {
    public static void main (String[]args){

        //index of
        String name = "alexa";
        name.indexOf("a");
        System.out.println(name.lastIndexOf("a"));

       /*
        //split
      String names = "Alexa, Siri, Julia";
      String [] namesSplit = names.split(",");
      for(int i =0; i< namesSplit.length; i++){
          System.out.println(namesSplit[i]);
      }


        //trim
        String names = "Alexa, Siri, Julia";
        String withWhiteSpace = "         space";
        System.out.println(withWhiteSpace.trim());
        String [] namesSplit = names.split(", ");
        for(int i =0; i< namesSplit.length; i++){
            System.out.println(namesSplit[i].trim());
        }

         //charArray
        String names = "Alexa";
        char[] elements = names.toCharArray();
        System.out.println(elements.length);

        //replaceAll
        String name = "alexa";
        String newName = name.replaceAll("ale", "bf");
        System.out.println(newName);

         //substring
        String name = "alexa";
        String newName = name.substring(0, 4);
        System.out.println(newName);

         //charAt
        String name = "alexa";
        char newName = name.charAt(2);
        System.out.println(newName);

        //concat
        String name = "alexa";
        System.out.println(name.concat("ndra"));
        */
    }
}
